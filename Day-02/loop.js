//  for in loop

const person= {name:"alauddin", age:25, address:{city:"Barisal", village:"Mongolhata"}}


for( let key in person){
    console.log(key)
}

    const colors = ["red", "green", "blue"];
    for (let color of colors) {
      console.log(color);
    }
        const numbers = [1, 2, 3, 4, 5];
    numbers.forEach(function(number) {
      console.log(number);
    });